% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{gamma_inference}
\alias{gamma_inference}
\title{Bayesian inference for the Wishart shape parameter, gamma (p = 1) case}
\usage{
gamma_inference(
  x,
  mu0 = 1,
  beta = 0,
  eta = 1,
  kappa = 0,
  nsamp = 10000L,
  tol = 1e-06,
  prnt = FALSE,
  max_em_iter = 1000L
)
}
\arguments{
\item{x}{Numeric vector of n scalar observations, each > 0}

\item{mu0}{Prior center for mu, must be > 0 (ignored if improper)}

\item{beta}{Gamma prior shape, must be >= 0 (ignored if improper)}

\item{eta}{Gamma prior rate, must be >= 0 (ignored if improper)}

\item{kappa}{Prior strength on mu, must be >= 0 (ignored if improper)}

\item{nsamp}{Number of posterior samples}

\item{tol}{Convergence tolerance for the EM mode finder}

\item{prnt}{If TRUE, print EM iterates}

\item{max_em_iter}{Maximum number of EM iterations}
}
\value{
A list with \code{results} (alpha_samples, mu_samples, ahat,
  theoretical_acpt_rate, empirical_acpt_rate) and \code{statistics}
  (xbar, muhat, log_det_geometric_mean, cover_shape, cover_rate,
  elapsed_seconds). If a convergence failure is caught internally,
  returns \code{list(error = "...")} instead.
}
\description{
Given \code{x_1,...,x_n ~ iid Gamma(alpha, alpha/mu)} -- the p = 1
reduction of \code{X_i ~ Wishart_p(2*alpha, Sigma)} -- computes the
posterior of (alpha, mu) under either an improper or proper prior.
Scalar (p = 1) analogue of \code{wishart_inference()}: takes a plain
numeric vector instead of a p x p x n array, since each observation
is already a positive scalar. There is no unifying dispatch with
\code{wishart_inference()} -- call this directly for scalar data.
}
