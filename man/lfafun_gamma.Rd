% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{lfafun_gamma}
\alias{lfafun_gamma}
\title{Log unnormalized posterior of alpha, gamma (p = 1) case}
\usage{
lfafun_gamma(a, n, xbar, ldetxbarg, beta = 0, eta = 1, kappa = 0, mu0 = 1)
}
\arguments{
\item{a}{Shape parameter alpha, must be > 0}

\item{n}{Number of scalar observations}

\item{xbar}{Sample mean of the observations}

\item{ldetxbarg}{mean(log(x)) -- scalar analogue of ldetxbarg}

\item{beta}{Gamma prior shape, must be >= 0 (0 = improper)}

\item{eta}{Gamma prior rate, must be >= 0}

\item{kappa}{Prior strength on mu, must be >= 0 (0 = improper on mu)}

\item{mu0}{Prior center for mu, must be > 0}
}
\value{
log f*(alpha), or -Inf if a <= 0
}
\description{
The p = 1 reduction of the Wishart model: X_i ~ Gamma(alpha, alpha/mu).
Auto-detects improper vs. proper exactly as the p >= 2 functions do
(beta = 0, eta = 1, kappa = 0 = improper), except kappa = 0 alone
(with beta > 0) is also valid here -- a proper prior on alpha with a
flat/improper treatment of mu -- unlike the p >= 2 backend, which
requires kappa >= 1 whenever the prior isn't fully improper.
}
