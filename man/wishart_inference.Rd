% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{wishart_inference}
\alias{wishart_inference}
\title{Bayesian inference for the Wishart shape parameter}
\usage{
wishart_inference(
  X,
  mu0_ = NULL,
  beta = 0,
  eta = 1,
  kappa = 0,
  nsamp = 10000L,
  tol = 1e-06,
  prnt = FALSE,
  max_em_iter = 1000L,
  max_nr_iter = 100L
)
}
\arguments{
\item{X}{Cube of n Wishart observations, dimensions (p, p, n)}

\item{mu0_}{Prior center matrix (p x p); omit for improper prior}

\item{beta}{Gamma prior shape, must be > 1 (ignored if improper)}

\item{eta}{Gamma prior rate parameter (ignored if improper)}

\item{kappa}{inv-Wishart prior strength, must be >= 1 (ignored if improper)}

\item{nsamp}{Number of posterior samples}

\item{tol}{Convergence tolerance for the EM mode finder}

\item{prnt}{If TRUE, print EM iterates}

\item{max_em_iter}{Maximum number of EM iterations}

\item{max_nr_iter}{Maximum number of inner Newton-Raphson iterations}
}
\value{
A list with \code{results} (alpha_samples, mu_samples, ahat,
  theoretical_acpt_rate, empirical_acpt_rate) and \code{statistics}
  (xbar, muhat, log_det_geometric_mean, cover_shape, cover_rate,
  elapsed_seconds). If a convergence failure is caught internally,
  returns \code{list(error = "...")} instead.
}
\description{
Given \code{X_1,...,X_n ~ iid Wishart_p(2*alpha, Sigma)}, computes the
posterior of (alpha, mu) where mu = alpha*Sigma, under either an
improper or proper prior. Runs the full pipeline: sufficient
statistics, EM mode finding, and rejection sampling.
}
