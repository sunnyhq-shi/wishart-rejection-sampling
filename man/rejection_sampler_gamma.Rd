% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rejection_sampler_gamma}
\alias{rejection_sampler_gamma}
\title{Rejection sampler for the joint posterior of (alpha, mu), gamma (p = 1) case}
\usage{
rejection_sampler_gamma(
  n,
  xbar,
  ldetxbarg,
  ahat,
  mxlfa,
  beta = 0,
  eta = 1,
  kappa = 0,
  mu0 = 1,
  nsamp = 10000L
)
}
\arguments{
\item{n}{Number of scalar observations}

\item{xbar}{Sample mean of the observations}

\item{ldetxbarg}{mean(log(x))}

\item{ahat}{Posterior mode from \code{mode_alphaEM_gamma()[1]}}

\item{mxlfa}{log f*(ahat) from \code{mode_alphaEM_gamma()[2]}}

\item{beta}{Gamma prior shape, must be >= 0 (0 = improper)}

\item{eta}{Gamma prior rate, must be >= 0}

\item{kappa}{Prior strength on mu, must be >= 0 (0 = improper on mu)}

\item{mu0}{Prior center for mu, must be > 0}

\item{nsamp}{Number of posterior samples (default 10000)}
}
\value{
A list with \code{alpha_sample}, \code{mu_sample},
  \code{empirical_acpt_rate}, \code{theoretical_acpt_rate}
}
\description{
Scalar (p = 1) analogue of \code{rejection_sampler()}. mu | alpha, x is
drawn from reciprocal-Gamma(nk_eff*alpha, alpha*(n*xbar+kappa*mu0)) --
see the C++ doc comment for the derivation.
}
