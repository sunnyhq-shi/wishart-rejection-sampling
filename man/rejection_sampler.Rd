% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/RcppExports.R
\name{rejection_sampler}
\alias{rejection_sampler}
\title{Rejection sampler for the joint posterior of (alpha, mu)}
\usage{
rejection_sampler(
  ahat,
  mxlfa,
  lambda,
  nu_star,
  p,
  n,
  xbar,
  ldetxbarg,
  mu0_ = NULL,
  beta = 0,
  eta = 1,
  kappa = 0,
  nsamp = 10000L
)
}
\arguments{
\item{ahat}{Posterior mode from \code{mode_alphaEM()[1]}}

\item{mxlfa}{log f*(ahat) from \code{mode_alphaEM()[2]}}

\item{lambda}{Covering Gamma rate}

\item{nu_star}{Covering Gamma shape (= ahat*lambda + 1)}

\item{p}{Dimension}

\item{n}{Number of Wishart observations}

\item{xbar}{Sample mean matrix (p x p)}

\item{ldetxbarg}{Log geometric mean of determinants}

\item{mu0_}{Prior center matrix; omit for improper prior}

\item{beta}{Gamma prior shape; 0 = improper, must be > 1 if proper}

\item{eta}{Gamma prior rate; default 1.0}

\item{kappa}{inv-Wishart prior strength; 0 = improper, must be >= 1 if proper}

\item{nsamp}{Number of posterior samples (default 10000)}
}
\value{
A list with \code{alpha_sample}, \code{mu_sample},
  \code{empirical_acpt_rate}, \code{theoretical_acpt_rate}
}
\description{
Requires the posterior mode \code{ahat} and \code{mxlfa} = log f*(ahat)
from \code{mode_alphaEM()}, plus the covering Gamma parameters
\code{lambda} and \code{nu_star}. Prior type is auto-detected from
parameters, exactly as in \code{mode_alphaEM()}.
}
